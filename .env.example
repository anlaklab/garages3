# Garage S3 Environment Variables
# Set these in Dokploy environment variables

# RPC Secret (32 bytes hex) - Generate with: openssl rand -hex 32
RPC_SECRET=

# Admin Token - Generate with: openssl rand -base64 32
# Used by both Garage and the WebUI
ADMIN_TOKEN=
